<div align="center">
  <img src="CleanMacForDevs/Assets.xcassets/AppIcon.appiconset/CleanMacForDevs-iOS-Default-1024x1024@1x.png" alt="CleanerForDevs icon" width="150"/>
</div>


# CleanerForDevs

A macOS app that helps developers reclaim disk space by removing build artifacts, caches, and dependency folders from their projects. Supports **Flutter, Xcode, Node.js, Python, Rust, Android, React Native, React, Swift Package Manager, Docker, Gradle Cache, npm Global**, and a **General Analysis** mode for system-level directories.

## How it works

1. Open the app — a **Welcome screen** shows all supported technologies in a grid for quick access.
2. Select a technology from the sidebar or the Welcome screen.
3. Click **Select folders** and choose the root folders of the projects you want to manage.
4. Enable or disable each project with the toggle (only enabled projects are cleaned).
5. Press **Run clean**. The app shows a progress overlay while cleaning and displays a summary — number of items removed and disk space freed — when done.
6. Selected paths are persisted with secure bookmarks. Just reopen the app and run again whenever needed.

The app also lives in the **menu bar**, showing live RAM, storage, CPU, and battery stats at a glance.

---

## What is removed

### Flutter
Detects projects that contain `pubspec.yaml`.

| Artifact | Description |
|---|---|
| `build/` | Flutter build output |
| `.dart_tool/` | Dart toolchain cache |
| `pubspec.lock` | Dependency lock file |
| `ios/Pods/` | CocoaPods dependencies |
| `ios/Podfile.lock` | CocoaPods lock file |
| `ios/Gemfile.lock` | Bundler lock file |

These are regenerated by `flutter pub get`, `pod install`, or a new build.

---

### Xcode
Select your `~/Library/Developer/Xcode` folder once; the selection is saved. On subsequent runs a confirmation dialog shows how much space will be freed before deleting.

| Artifact | Description |
|---|---|
| `DerivedData/` | Compiled build intermediates |
| `Index/` | Xcode symbol index |
| `Archives/` | Old `.xcarchive` files |

These are regenerated by Xcode on the next build.

---

### Node.js
Detects projects that contain `package.json`. The app reads the package file to identify the framework (Next.js, Nuxt, SvelteKit, Angular, Vue, React) and shows a badge per project.

| Artifact | Description |
|---|---|
| `node_modules/` | Installed package dependencies |
| `dist/` | Build output |
| `build/` | Alternative build output |
| `.next/` | Next.js cache |
| `.nuxt/` | Nuxt.js cache |
| `.svelte-kit/` | SvelteKit cache |
| `.turbo/` | Turborepo cache |
| `coverage/` | Test coverage reports |
| `.parcel-cache/` | Parcel bundler cache |

Re-install with `npm install` / `yarn` / `pnpm install`.

---

### Python
Detects projects that contain `requirements.txt`, `pyproject.toml`, `setup.py`, or `Pipfile`.

| Artifact | Description |
|---|---|
| `venv/`, `.venv/`, `env/`, `.env/` | Virtual environments |
| `.pytest_cache/` | pytest cache |
| `dist/`, `build/` | Package build artifacts |
| `.mypy_cache/` | mypy type-checker cache |
| `.ruff_cache/` | Ruff linter cache |
| `__pycache__/` | Python bytecode (scanned recursively in all subdirectories) |

---

### Rust
Detects projects that contain `Cargo.toml`. The `target/` directory in Rust projects is notorious for consuming several gigabytes. If the total size is ≥ 1 GB, the app shows a confirmation dialog with the exact size before deleting.

| Artifact | Description |
|---|---|
| `target/` | All build artifacts (debug, release, incremental) |

Re-generated by `cargo build`.

---

### Android
Detects projects that contain `build.gradle` or `build.gradle.kts`. The app identifies the project language (Kotlin, Java, or Kotlin+Java) and shows a badge per project.

| Artifact | Description |
|---|---|
| `.gradle/` | Gradle build cache |
| `build/` | Build output directory |
| `app/build/` | App-specific build artifacts |
| `.kotlin/` | Kotlin compiler cache |
| `captures/` | Android Studio profiler captures |
| `.cxx/` | NDK/C++ build artifacts |

Re-generated by `./gradlew build`.

---

### React
Detects React projects via `package.json`. The app identifies the toolchain (Next.js, Gatsby, Remix, Vite, Create React App) and shows a badge per project.

| Artifact | Description |
|---|---|
| `node_modules/` | Installed dependencies |
| `build/` | Build output |
| `dist/` | Distribution files |
| `.next/` | Next.js cache |
| `.cache/` | Generic cache |
| `coverage/` | Test coverage reports |
| `.parcel-cache/` | Parcel bundler cache |
| `.turbo/` | Turborepo cache |
| `storybook-static/` | Storybook static build |

Re-install with `npm install` / `yarn` / `pnpm install`.

---

### React Native
Detects React Native projects via `package.json`. The app checks for Expo framework and shows a platform badge per project. Cleans artifacts from both iOS and Android sides.

| Artifact | Description |
|---|---|
| `node_modules/` | Node dependencies |
| `android/.gradle/` | Android Gradle cache |
| `android/build/` | Android build output |
| `android/app/build/` | Android app build |
| `ios/Pods/` | iOS CocoaPods dependencies |
| `ios/build/` | iOS build artifacts |
| `.metro/` | Metro bundler cache |
| `coverage/` | Test coverage reports |

Re-install with `npm install` and `cd ios && pod install`.

---

### Swift Package Manager
Detects SPM projects by selecting their root folders. If the total `.build` size is ≥ 1 GB, the app shows a confirmation dialog before deleting. Supports **dry-run preview** to inspect what will be removed before committing.

| Artifact | Description |
|---|---|
| `.build/` | All SPM build artifacts (debug, release, checkouts) |

Re-generated by `swift build`.

---

### Docker
Requires Docker Desktop to be installed. Grant access to `~/Library/Containers/com.docker.docker` once — the app then shows live disk usage broken down by VM disk image and logs. Instead of deleting files directly, the app provides ready-to-copy CLI commands so you stay in full control.

| Command | What it does |
|---|---|
| `docker system df` | Show Docker disk usage |
| `docker system prune -a --volumes -f` | Remove all unused data (images, containers, volumes, build cache) |
| `docker image prune -a -f` | Remove all unused images |
| `docker container prune -f` | Remove all stopped containers |
| `docker volume prune -a -f` | Remove all unused volumes |
| `docker builder prune -a -f` | Remove all build cache |

Click any command card to copy it to the clipboard, then paste it in your terminal.

---

### Gradle Cache
Scans the global Gradle cache at `~/.gradle/caches`. Grant access once via the Open Panel; the app lists every versioned cache entry with its size so you can selectively delete the ones you no longer need.

| What is removed |
|---|
| Individual version entries inside `~/.gradle/caches/` |

Re-populated automatically by the next Gradle build.

---

### npm Global
Scans globally installed npm packages. Grant access to your global `node_modules` directory once (the app auto-detects paths for nvm, Homebrew Intel, and Homebrew Apple Silicon). Packages are listed with their sizes so you can remove specific ones without wiping everything.

| What is removed |
|---|
| Individual packages inside the global `node_modules/` directory |

Re-install any package with `npm install -g <package>`.

---

### General Analysis
Scans system-level user directories that accumulate space over time. Each category requires a one-time access grant (via the macOS Open Panel) to stay within the App Sandbox. Press **Scan Now** to calculate sizes, select the categories you want to clean, then press **Clean Selected**.

| Category | Path | What is removed |
|---|---|---|
| Old Downloads | `~/Downloads` | Files older than 30 days |
| Xcode DeviceSupport | `~/Library/Developer/Xcode/iOS DeviceSupport` | All contents |
| App Caches | `~/Library/Caches` | All contents |
| User Logs | `~/Library/Logs` | All contents |
| CoreSimulator Caches | `~/Library/Developer/CoreSimulator/Caches` | All contents |

The scan runs all categories in parallel. After cleaning, the app automatically rescans to show the updated sizes.

---

## Menu Bar
The app runs a lightweight menu bar extra that displays live system stats without opening the main window.

| Stat | Description |
|---|---|
| RAM | Total and available memory |
| Storage | Total and available disk space |
| CPU | Current CPU usage percentage |
| Battery | Charge level and charging state (Mac notebooks) |

---

## Architecture

- **macOS 14.0+** — uses `@Observable`, `NavigationSplitView`, `async/await`
- **App Sandbox enabled** — all file access goes through user-granted permissions and security-scoped bookmarks
- **MVVM** — each feature has its own `View` + `ViewModel`; shared services (`CleanExecutor`, `FolderPickerService`, `BookmarkRepository`, `SystemScanner`) are injected via initializer
- **Localization** — English and Brazilian Portuguese (pt-BR)
- **Custom SVG icons** — each technology has its own vector icon in the sidebar and Welcome screen

---

## Download

Latest release:
https://github.com/andrelucassvt/CleanMacForFlutter/releases
